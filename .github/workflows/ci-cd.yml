name: Test, Build and Deploy

on:
  push:
    branches: [ main, master ]

jobs:
  # 1. Запуск тестов и линтинга кода
  test-and-lint:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_DB: test_db
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_password
        ports: [5432:5432]

      redis:
        image: redis:7-alpine
        ports: [6379:6379]

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        pip install -r requirements.txt
        pip install flake8

    - name: Run flake8 linting
      run: |
        flake8 . --count --max-line-length=127 --statistics --exclude=migrations

    - name: Run tests
      env:
        DEBUG: "True"
        SECRET_KEY: "test-secret-key-${{ github.run_id }}"
        DB_NAME: test_db
        DB_USER: test_user
        DB_PASSWORD: test_password
        DB_HOST: localhost
        DB_PORT: 5432
        ALLOWED_HOSTS: "localhost,127.0.0.1"
        CELERY_BROKER_URL: "redis://localhost:6379/0"
        CELERY_RESULT_BACKEND: "redis://localhost:6379/0"
        TELEGRAM_BOT_TOKEN: "test:token"
      run: |
        python manage.py test --exclude-tag=telegram --noinput --verbosity=2

  # 2. Проверка сборки Docker-образов
  docker-build:
    runs-on: ubuntu-latest
    needs: test-and-lint
    if: success() && github.ref == 'refs/heads/main'

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Build Docker image
      run: |
        docker build -t habits-tracker .

    - name: Test Docker image
      run: |
        docker run --rm habits-tracker python -c "import django; print('Django available')"

  # 3. Автоматический деплой на сервер
  deploy:
    runs-on: ubuntu-latest
    needs: [test-and-lint, docker-build]
    if: success() && github.ref == 'refs/heads/main'

    steps:
    - name: Install SSHpass
      run: sudo apt-get install -y sshpass

    - name: Deploy to server
      run: |
        export SSHPASS=${{ secrets.SSH_PASSPHRASE }}
        
        sshpass -e ssh -o StrictHostKeyChecking=no ${{ secrets.SERVER_USERNAME }}@${{ secrets.SERVER_HOST }} << 'EOF'
          echo "🚀 Starting deployment..."
          cd /opt/habits-tracker
          
          # Останавливаем текущие контейнеры
          docker-compose down
          
          # Pull последних изменений
          git pull origin main
          
          # Перезапускаем контейнеры
          docker-compose up -d --build
          
          # Выполняем миграции
          docker-compose exec web python manage.py migrate --noinput
          
          # Собираем статические файлы
          docker-compose exec web python manage.py collectstatic --noinput
          
          echo "✅ Deployment completed successfully!"
        EOF
